function toggleMenu() {
    const menu = document.querySelector(".menu-links");
    const icon = document.querySelector(".hamburger-icon");
    menu.classList.toggle("open");
    icon.classList.toggle("open");
  }

  // todo
  // switch around about me section image and text
  // rewrite experience section
  // lose the borders on the tooolbox divs?
  // set up msg for contact me

  // projects
  // things up links
  // work out how to put text over images